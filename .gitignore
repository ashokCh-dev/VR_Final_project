# Large data + model artifacts (download from Kaggle datasets instead — see README)
vr_final_proj_dataset/
artifacts/
yolo/
*.pt
*.bin
*.zip
captions.json
*.log

# Windows download metadata
*Zone.Identifier
*:Zone.Identifier

# Python build/cache
__pycache__/
*.pyc
*.pyo
.ipynb_checkpoints/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Editor / OS junk
.vscode/
.idea/
.DS_Store

# Local secrets (the .kaggle dir lives in $HOME, but just in case)
.kaggle/
kaggle.json
access_token

# PDFs (the project statement is the only file we keep; ignore generated reports)
report/*.pdf
